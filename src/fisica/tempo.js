// Segundos para Minutos
const secondsToMinutes = (s) => (s * 1 / 60)

// Minuto para Hora
const minutesToHours = (min) => (min * 1 / 60)

// Minuto para Segundos
const minutesToSeconds = (min) => (s * 60)

// Hora para Minuto
const HoursToMinutes = (h) => (h * 60)
