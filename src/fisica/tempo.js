// Segundos para Minutos
const stomin = (s) => (s * 1 / 60)

// Minuto para Hora
const mintoh = (min) => (min * 1 / 60)

// Minuto para Segundos
const mintos = (min) => (s * 60)

// Hora para Minuto
const htomin = (h) => (h * 60)
